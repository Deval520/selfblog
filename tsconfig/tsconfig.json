{
	"compilerOptions": {
		"module": "commonjs",
		"esModuleInterop": true,
		"target": "es6",
		"noImplicitAny": true,
		"moduleResolution": "node",
		"sourceMap": true,
		"outDir": "../dist", // not to be compiled
		"baseUrl": "../", // modules are deployed to a single folder at run-time. modules can live in different directories. but build script will put them together.
		"paths": {
			"*": [ // all of the modules are live in those directories. this mapping is relative to baseUrl, for non-relative
				"node_modules/*",
				"src/types/*" // for add types to module that doesn't exit types '.d.ts'
			]
		}
	},
	"include": [
		"../src/**/*"
	]
}
// rootDirs. for relative imports like './'. modules are expected to exist next to it.